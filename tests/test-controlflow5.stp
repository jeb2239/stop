/*
 * test-controlflow5.stp
 * =====================
 */

def main = ():Int {
	var a:Int = 1;
        if (a == 0) {
		a = a - 2;
        }
	
	if (a == 1){
		printf("%d", a+1);
		a = a+1; 
	}

	var b:Int = 1;
	if (a >= 3) {
		b = a + 1;
	}else if (a <= 2){
		b = a;
	}
	
	printf("%d",b);
        
	if (a!=b){
		a = b+1;
	} else {
		var d:Int = 3;
	}

	printf("%d", d);
	return 0;
}
