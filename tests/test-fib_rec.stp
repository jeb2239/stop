/*
 * test-fib_rec.stp
 * ====================
 */

def fib = (i:Int):Int {
	printf("%d\n", i);
	return 0;
	/*
	if (i > 0) {
		return 1;
	} else {
		return 0;
	}
	} else if (i == 1) {
		return 1;
	} else {
		return fib(i-1) + fib (i-2);
	}
	*/
}

def main = ():Int {
	/*
	printf("%d\n", fib(10));
	printf("%d\n", fib(9));
	printf("%d\n", fib(8));
	*/
	printf("%d\n", fib(1));
	printf("%d\n", fib(0));
	return 0;
}
