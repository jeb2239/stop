/*
 * test-array1.stp
 * =====================
 */

def main = ():Int {
        var a:Float[] = Float[6]();
        printf("%d",sizeof(a));
       	
        printf("%d",sizeof(a[0]));
	//	a[0]='c';
		//a[1]=1;
	//	printf("%c", a[0]);
	//	printf("%d", a[1]);
        return 0;
}